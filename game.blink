option ClientOutput = "src/ReplicatedStorage/TowerGame/Network/BlinkClient.luau"
option ServerOutput = "src/ServerScriptService/Server/Network/BlinkServer.luau"

struct ClientWinMessageInfo {
    PlayerName: string,
    EndingName: string,
    Time: f32,
    BoostsUsed: map { [string]: string },
    AllJumps: boolean,

    Color: Color3,
    FancyFont: boolean,

    Global: boolean
}

event RestartRequested {
    From: Client,
    Type: Reliable,
    Call: SingleAsync
}

function RequestEverpresentCOs {
    Yield: Coroutine,
    Return: (boolean, Instance(Folder)?)
}

event UpdateTowerTimer {
    From: Server,
    Type: Unreliable,
    Call: SingleAsync,
    Data: f32
}

event UpdateTowerRush {
    From: Server,
    Type: Reliable,
    Call: SingleAsync,
    Data: (u8, u8)
}

event LoadTower {
    From: Server,
    Type: Reliable,
    Call: SingleAsync,
    Data: (string, boolean, boolean)
}

event UnloadTower {
    From: Server,
    Type: Reliable,
    Call: SingleAsync
}

event GameAnnouncement {
    From: Server,
    Type: Reliable,
    Call: SingleAsync,
    Data: (string, Color3, boolean?)
}

event GameWinAnnouncement {
    From: Server,
    Type: Reliable,
    Call: SingleAsync,
    Data: ClientWinMessageInfo
}

event PlayerDataUpdate {
    From: Server,
    Type: Reliable,
    Call: SingleAsync,
    Data: unknown
}

event SetAllJumpsMode {
    From: Client,
    Type: Reliable,
    Call: SingleAsync,
    Data: boolean
}

event ExitTowerRequest {
    From: Client,
    Type: Reliable,
    Call: SingleAsync
}

event SetPracticeMode {
    From: Client,
    Type: Reliable,
    Call: SingleAsync,
    Data: boolean
}

function GetPlayerData {
    Yield: Coroutine,
    Data: u32,
    Return: unknown
}
